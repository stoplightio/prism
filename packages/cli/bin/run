#!/usr/bin/env node

const yargs = require('yargs');

yargs
  .scriptName("prism")
  .commandDir('../src/commands', { extensions: ['ts'] })
  .demandCommand()
  .version(false)
  .help(false)
  .wrap(yargs.terminalWidth())
  .argv
