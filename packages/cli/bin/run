#!/usr/bin/env node

require('yargs')
  .scriptName("prism")
  .commandDir('../src/commands', {
    extensions: process.env.NODE_ENV !== 'production' ? ['js', 'ts'] : ['js'],
  })
  .demandCommand()
  .version(false)
  .help(false)
  .argv
